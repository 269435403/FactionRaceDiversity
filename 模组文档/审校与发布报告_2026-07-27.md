# 审校与发布报告（2026-07-27）

## 审校范围

- `About/About.xml`、两个内置 FactionDef、简体中文与英文翻译。
- 全部 13 个 C# 源文件、Harmony 目标、设置迁移、Race/异种选择与诊断路径。
- 16 个 Race 兼容配置、共享派系列表、条件异种、298 组 PawnKind 映射与 7 组同 Race 阵营扩展变体。
- Wiki、测试文档、构建配置与 GitHub 发布内容。

## 本地依据

- RimWorld 1.6 本地反编译源码与 Def 索引。
- Steam Workshop 已安装来源模组及“模组制作工作台/创意工坊其他模组及源码”中的 1.6 来源 XML。
- Miho 本体 `1.6/Defs/FactionDefs/Faction_MihoPirate.xml`、`1.6/Defs/BioDesu/XenotypeDefs.xml` 与 `1.6/Defs/PawnKindDefs/PawnKinds_Miho.xml`。

## 已修正

1. 共享派系列表补入 Miho 本体人形派系 `Miho_Faction_Supremacist`，总数由 81 增至 82。
2. Miho 原生异种池补入具有正生成概率的 `Xeno_CelestialMiho_Desert`。
3. 最终生成入口发生异常时恢复完整 `PawnGenerationRequest`，避免半修改请求继续进入原版生成。
4. Harmony 仅在 `PatchAll` 成功后标记完成，初始化失败不再留下错误完成状态。
5. 特殊袭击角色反射检查失败时记录一次错误，并保留原版策略结果，不再静默吞错。
6. 静态测试、About、Wiki、兼容规则、总文档、注意事项、测试矩阵与 README 已同步。

## 自动验证

- XML：6 个文件全部可解析。
- 静态回归：全部通过。
- Release 构建：0 警告、0 错误。
- DLL：发布包与运行目录 SHA-256 一致。
- 发布清理：无 `bin/`、`obj/`、PDB、`__pycache__`、原版 DLL 或 Harmony DLL。

## 最小运行状态

运行目录 `Mods/yyyyy_MixedPeoplesFactions/` 仅保留：

- `About/`
- `1.6/`

同级测试用文化多样性模组已移入 `Mods/临时存放/yyyyy_FactionCulturalDiversity_Steam/`，因此 Mods 顶层当前只有本模组具有直接 `About/About.xml`。

## 尚需游戏内验收

本轮没有启动 RimWorld。请重点执行：

1. 仅 Core + Harmony + 本模组：确认加载无 XML、程序集与 Harmony 红字。
2. Miho 本体 + Star Ring + Biotech：确认 `Miho_Faction_Supremacist` 可调整，异种池包含沙漠天狐。
3. 启用 Odyssey：确认只额外出现虚空天狐。
4. Human 0%、Miho 100%：验证普通袭击、军事支援、访客、商队商人、护卫与人形商品。
5. 保存并重载，确认阵营 Race/异种比例保持。
