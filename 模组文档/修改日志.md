# 修改日志

## 2026-08-02：玩家自定义异种预设自动发现

- 自动读取原版磁盘预设目录中的 `CustomXenotype`，在每个正权重 Humanlike Race 的独立异种池中显示“玩家制作的预设”；新预设默认显示为 0，只有玩家实际调整后才写入设置，不改变现有生成比例或膨胀设置文件。
- Def 异种与玩家预设使用统一候选和权重抽取，但生成请求严格分别写入 `ForcedXenotype` 或 `ForcedCustomXenotype`，并清理普通 PawnGroup 的旧软选择字段。
- 已有硬指定 `ForcedCustomXenotype` 的任务、场景或第三方请求继续原样保留；玩家预设不伪装成 `XenotypeDef`，也不写入 `FactionDef.xenotypeSet`。
- 玩家预设没有 Race/HAR 归属元数据：Human 可直接设置；非 Human Race 只在玩家显式设置正权重后使用。含禁用暴力基因的预设不会进入战斗请求。
- 预设文件覆盖保留权重，重命名视为新预设；缺失预设立即停止参与抽取，唯一正权重预设缺失时恢复默认安全池。补充中英文设置文本、README、维护规则和专项测试矩阵。

## 2026-07-29：纯工作区迁移与工程路径记录

- 开发主副本迁移至 `D:\RimWorldModding\projects\yyyyy_MixedPeoplesFactions`；本条仅记录工作区位置与工程构建路径调整。
- `Source/MixedPeoplesFactions/MixedPeoplesFactions.csproj` 的 `RimWorldDir` 继续允许通过外部 MSBuild 属性传入；未传入时改用本机默认 `D:\steam\steamapps\common\RimWorld`，不再依赖项目所在目录的相对层级。
- 保留既有程序集引用优先级与 `OutputPath`；本次没有玩法、Def 或运行副本变化，也未执行构建、运行或同步。
- `模组逐文件白话说明.md` 未更新：文件结构及玩家可改性均未变化。

## 2026-07-27：GitHub 与 Steam 发布文案整理

- 重写 GitHub README，使用五张双语抬头图组织简介、内容、兼容性、问答和仓库链接。
- 新增中英文功能说明、完整兼容模组名称列表、加载顺序、常见问题与显著的不稳定性警告；公开文案不使用 PackageID 代替模组名称。
- 导出可直接复制的中英文 Steam 创意工坊 BBCode 描述，并使用公开 GitHub 原始图片地址作为抬头图图床。
- 中文发布信息加入 QQ 群 672646837；英文发布信息不包含 QQ 群。

## 2026-07-27：修复百族阵营特殊袭击 post-gear 验证耗尽

- 确认 `Generated pawn didn't pass validator check (post-gear)` 与本模组相关：原版袭击策略会在装备生成后校验工兵、破墙手或仪式施术者，本模组的手动映射与通用后备此前没有像自动匹配路径一样复核这些特殊角色标记。
- 三条替换路径现在统一要求保留 `canBeSapper`、`isGoodBreacher` 与 `isGoodPsychicRitualInvoker`；普通战斗角色仍可使用瓦尔基里/派卡萨斯的单一通用 PawnKind，但特殊策略角色不得再降级为普通角色。
- 若某个 100% Race 没有合格特殊角色，本次特殊请求保留原 PawnKind，而不是让原版验证器连续失败 70/100 次；普通袭击、援军与非特殊成员仍遵守配置比例。
- 已补充静态回归、测试矩阵与维护规则；Release 构建后需在游戏内分别复测普通支援、工兵袭击、破墙袭击与灵能仪式围攻。

## 2026-07-27：修复 Miho 智人种与 Epona-Milira 混血 Race

- Miho 兼容配置显式排除 `Baseliner`，阻止阵营基线或同源归属汇总把智人种重新加入美狐异种条。
- 核对 Epona 的条件内容包，确认“瓦尔基里”与“派卡萨斯”分别是 `Alien_Epona_Milira`、`Alien_Unicorn_Milira`，不是基础 Epona/Unicorn 的显示别名。
- 新增两个独立兼容配置，分别锁定 `Xeno_EponaMilira` 与 `Xeno_UnicornMilira`，排除基础 Epona 三异种、另一混血异种和 Baseliner。
- 来源模组只为两种混血 Race 提供一个通用 PawnKind；新增仅限这两个配置的通用角色例外，使其在 Human 0%、100% 权重时可承担平民、战斗、商人和领袖请求。
- 分析创建世界红字：Ratkin 神祇语法缺词与本模组无关；领袖 Pawn 生成耗尽并返回 null 可能由此前混血 Race 缺少角色映射后回退 Human、再套用错误异种池触发，本轮已针对该链路修复。
- 静态回归与 Release 构建通过；仍需实际创建世界复测红字是否消失。

## 2026-07-27：全量审校、Miho 遗漏补充与发布整理

- 对 16 个 Race 配置、共享派系列表、条件异种、PawnKind 映射和本地 1.6 来源 XML 进行交叉审校；补入 Miho 本体派系 `Miho_Faction_Supremacist`，共享表由 81 项增至 82 项。
- 补入 Miho 本体中具有正生成概率的沙漠天狐 `Xeno_CelestialMiho_Desert`；Miho 原生池现在覆盖天狐、极地、沙漠、高地、高魅力与 Odyssey 条件虚空天狐。
- Harmony 最终生成入口在异常时恢复完整原请求；Harmony 初始化只在 `PatchAll` 成功后标记完成；特殊袭击角色反射检查失败时记录一次错误而不再静默吞掉。
- 扩充静态回归断言、总文档、兼容规则、测试矩阵、修改注意事项和 GitHub README；重新生成 Wiki 并完成 Release 构建。
- 本轮未启动 RimWorld；Miho 本体派系、沙漠天狐和全部可选组合仍需按测试矩阵做游戏内验收。

## 2026-07-27：HAR 第四批阵营与异种扩展兼容

- 完成 `HAR系列` 下除“已兼容”外 13 个目录的逐项核对：新增 11 个 Humanlike FactionDef、OA 鼠族基因扩展 11 个异种、绮罗缅因/橘猫/布偶/暹罗 4 个异种，以及米莉拉 Gene Patch 的 `MiliraXenotype`。
- 将兼容派系列表改为共享 `FRD_FirstPartySupportedFactions`，补齐此前遗漏的 Ratkin、MoeLotl、Epona、Kiiro、Milira、Nivarian、Wolfein 等本体派系；15 个 Race 配置现在显式覆盖 80 个本模组、原版/DLC 与来源模组派系标识。
- 为萌螈血宗、鼠族军阀、鼠族骑士团、无律鼠邦和鼠族地下游击队补齐 13 组条件 PawnKind 映射及四类后备；原有金鸢尾兰与米莉拉帝国条件映射一并纳入变体机制。
- 新增同 Race 多阵营扩展变体选择：只启用一个扩展时保持原有“扩展兵种覆盖”行为；同时启用多个扩展时按目标派系稳定选择一个完整兵种变体，避免最后加载项覆盖所有派系。扩展自身派系优先使用同 PackageID 变体。
- 米莉拉 Gene Patch 启用时条件加入 `MiliraXenotype` 并排除遗留 `Baseliner`；四个绮罗猫型异种和 OA 鼠族 11 个异种均使用各自 PackageID 条件加载。
- `Milira: Wings of Democracy` 仅提供任务专用米莉拉 PawnKind 与机械单位，没有可安全作为通用派系后备的新阵营表；`Wolfein Race GFI Expand` 只提供机械 PawnKind。两者已核对并明确排除，不把任务人物或机械单位误纳入普通 Humanlike 兼容。
- 更新 About、兼容规则、总文档、修改注意事项、第二阶段测试矩阵与静态测试；Release 构建 0 警告、0 错误。游戏内 80 派系交叉配置、多扩展并存和新增异种池仍需启动 RimWorld 验收。
## 2026-07-27：第三批 HAR 种族与阵营扩展兼容

- 新增 Gloomy Dragonian、Kurin HAR、Moosesian 与 Yuran 的 6 个 Race 兼容配置；Yuran 的普通种、Miko 与 BlackSnake Miko 按三个真实 ThingDef Race 分开建表。
- 新增 Dragonian 双异种、Kurin 基线、Moosesian Woodland/Flatland 与 Yuran/Miko/BlackSnake 原生异种登记；来源 Def 未实际加载时仍按现有安全基线回退，不伪造 Def。
- 将本批 16 个 Humanlike FactionDef 加入全部兼容配置，现有 15 个 Race 配置共覆盖 46 个派系标识和 13 组来源角色。
- 金鸢尾兰鼠族阵营不重复注册 Ratkin：在 Ratkin 本体配置末尾条件覆盖 13 组精确映射、四类后备，并条件追加 `Ratkin_OA`，使扩展启用时普通派系可生成金鸢尾兰专属 PawnKind。
- 米莉拉帝国不重复注册 Milira：条件使用帝国公民、星际陆战队、判官、围攻陆战队等扩展 PawnKind；明确继续排除 `Milian_*` 机械单位和其他非 `Milira_Race` 兵种。
- 新增 `fallbackOverrides` 兼容字段。条件目标缺失、Race 不匹配或 PawnKind 不安全时保留种族本体后备，避免可选阵营扩展卸载后破坏已有配置。
- 更新 About、模组总文档、种族兼容基本规则、修改注意事项、第二阶段测试矩阵和静态测试；全部新增 Race、Xenotype 与 PawnKind 引用已对照用户提供的来源 XML。
- Release 构建 0 警告、0 错误，静态测试全部通过；游戏内单 Race 100%、Human 0%、袭击/援军/访客/商队与扩展开关矩阵仍需实际启动 RimWorld 验收。

## 2026-07-27：修复 Gene Patch 智人种滑条与战斗组平民

- 修复 Kiiro Race Gene Patch 与 Wolfein Race Gene Patch 启用后仍显示“智人种”滑条：兼容 Def 新增条件异种排除表，在汇总 PawnKind、Faction、同源异种和 HAR 白名单后统一移除 `Baseliner`，并阻止空池回退把它再次加入。
- 排除只随对应 Gene Patch 生效；仅启用 Kiiro/Wolfein 本体时仍保留其原生 `Baseliner` 基线。
- 修复袭击和军事支援中出现平民 PawnKind：外层 `Combat` PawnGroup、`MustBeCapableOfViolence` 或来源战士任一成立时，同 Race 保留、精确映射、自动匹配和通用后备均强制目标 `isFighter == true`。
- 平民精确映射在战斗组中会被拒绝并继续寻找同 Race 战斗候选；战斗后备不再降级到平民。必需袭击角色检查也会验证映射后的目标 PawnKind。
- 更新种族兼容基本规则、项目约束和静态回归检查；重新构建 DLL。

## 2026-07-27：第二批 HAR 与 Gene 扩展兼容

- 新增 Epona Dynastic Rise、Kiiro Race、Milira Race、Nivarian Race、Wolfein Race 的 7 个 Race 兼容配置；Epona 的 `Alien_Epona`、`Alien_Destrier`、`Alien_Unicorn` 分开建立 PawnKind 映射和四类后备。
- Ratkin Gene Expanded 不创建新 Race 配置，而是在现有 Ratkin 配置中条件追加 8 个扩展异种；保留其官方 `Solaris.RatkinRaceMod` 分支，不把未提供来源本体的非官方 Ratkin 分支擅自声明为已支持。
- Kiiro Race Gene Patch 仅在自身启用时向 `Kiiro_Race` 条件追加 `KiiroXenotype`；Kiiro 本体单独启用时不引用不存在的异种。
- Wolfein Race Gene Patch 仅在自身启用时向 `Wolfein_Race` 条件追加 `Wolfein_Xenotype` 与 `Wolfein_Xenotype_PureBlood`，不重复注册 Race。
- Nivarian Race 登记 Lumin、Pulsar、Aegis 三个 Biotech 条件异种；Milira Race 按来源数据使用 `Baseliner`，不伪造自定义 XenotypeDef。
- 明确排除没有普通具体 PawnKind 的 `Milian_Race`、Epona 条件混血 Race，以及 Milira/Wolfein 机械单位。
- 为新增兼容补齐 30 个标准 Humanlike FactionDef、13 组原 PawnKind 映射、自动匹配和通用平民/战斗/商人/领袖后备；Milira 商人与 Destrier 商人/领袖按规则使用同 Race 安全角色降级，不混入其他 Race。
- 更新 `About.xml` 的可选 `loadAfter`、模组总文档、兼容规则、注意事项、第二阶段测试矩阵与静态测试；新增配置的外部 PawnKind/Race 引用已逐项对照来源 XML。
- 当前自动静态测试全部通过；本批新增兼容仍需在 RimWorld 内完成单 Race 100%、Human 0%、商队、援军、访客、异种池和多 Race 混合验收。

## 2026-07-27：游戏内验收全部通过

- 玩家确认本轮全部游戏内测试通过：两个百族阵营的原始规则永久均分且不绿色置顶；文明外来者 Ratkin/Axolotl 50/50、Human 0% 的袭击与商队不再残留 Human；直接比例面板及异种比例显示正常。
- 本轮实现确定为后续兼容基线。以后新增种族或派系兼容时，必须遵守 `模组文档/种族兼容基本规则.md`，尤其检查普通队伍最终生成封口、`MustBeCapableOfViolence` 与 `isFighter` 的区别、商队人形商品的外层派系以及四类通用后备 PawnKind。


## 2026-07-27：永久均分原始规则、直接比例面板与普通队伍严格封口

- 设置 schema 升级为 6；两个内置百族阵营的“遵循原始规则”重新定义并实现为所有当前 Humanlike Race 永久等权，新增 Race 后自动重新均分，但原始均分状态不绿色、不置顶。
- 删除“自定义种族比例”和“自定义异种比例”两个启用复选框；受支持阵营始终直接显示 Race 比例，正权重 Race 自动显示独立异种比例，首次拖动即建立自定义配置。
- 旧仅异种配置自动迁移到默认 Race 的独立异种池；旧设置中已写入但启用标记丢失的非默认 Race 权重会恢复为有效直接比例配置。
- 将 Race/PawnKind/Xenotype 选择移动到 `GenerateOrRedressPawnInternal` 的最终 Prefix，并以最低 Prefix 优先级封口；原版 120 次生成重试不会再把已选 Ratkin/Axolotl 恢复成 Human。
- 修正 `MustBeCapableOfViolence` 被错误等同于 `PawnKindDef.isFighter` 的问题；原版 `Villager`、`Town_Councilman` 等非战斗组角色现在可以使用对应种族映射。
- PawnGroup 上下文现在向商队中的人形商品提供外层商队派系配置，解决奴隶等商品先以随机来源派系生成、随后加入商队而绕过比例的问题。驮兽和动物仍不参与替换。
- Ratkin 与 MoeLotl 兼容 Def 新增通用平民、战斗、商人、领袖后备；精确映射和自动匹配均失败时，普通队伍使用同 Race 后备，不再保留 Human。
- 新增 `模组文档/种族兼容基本规则.md`，以后所有 Race、PawnKind、Xenotype、Faction 兼容必须按该规则维护。


## 2026-07-27：首批手动兼容与原始规则修正

- 新增可扩展的 Race 兼容 Def：手动替换必须同时命中目标阵营、目标 Race 和原 PawnKind，缺少任一层时回到自动匹配，自动匹配仍失败则保留原人物。
- 首批为 NewRatkinPlus 与 MoeLotl Race 建立 Core、Royalty、Ideology、Biotech、Anomaly、Odyssey 人类阵营兼容表，并明确登记各自原生异种。
- 为文明外来者等常见阵营补齐平民与领袖映射，重点解决 `Villager`、`Town_Councilman` 无候选而保留 Human 的问题。
- 诊断不再把商队驮兽等非人形 Pawn 计作“意外种族”。
- “遵循原始规则”现在对两个示例阵营也真正删除活动配置；schema 5 会移除旧版本未修改的自动均分配置，因此原始规则阵营不再变绿或置顶。
- 新安装不再自动给两个示例阵营创建活动配置；旧设置文件中真实存在的第一阶段异种权重仍可迁移。
- About 描述已明确：不受支持的特殊角色会尝试自动匹配，仍不兼容时保留原人物。


## 2026-07-26：根据游戏内复测再次修正

- 修复两个内置百族阵营点击“遵循原始规则”后即时回到 Human 100%：内置阵营的原始规则现在明确为所有已发现 Race 自动均分，按钮会直接重建均分配置而不是删除配置。
- 修复鼠族 PawnKind `xenotypeSet` 中零概率占位项被误认作鼠族原生异种：只有 `chance > 0` 的条目才建立归属，因此 Baseliner（智人种）和 Sanguophage（赫血种）不会再出现在鼠族异种滑条中。
- 扩大 PawnGroup 软异种上下文到所有人形阵营队伍类型，避免自定义/支援队伍因未被识别为 Combat、Trader、Peaceful 而把预选 Human 异种当成任务硬限制，从而保留 Human。
- 普通 PawnGroup 不再因固定称号、允许异种列表等组内约束直接跳过 Race 替换；真正的开局、新生儿、变异体和 CreepJoiner 仍保持安全排除。
- 开发者报告新增实际生成了零权重 Race 的计数和具体 PawnKind 回退明细，便于继续定位任何残余生成路径。
## 2026-07-26：修复异种串池、特殊袭击红字与自动均分

- 设置 schema 升级为 4；百族同盟与粗野的百族同盟默认启用所有已发现 Race 自动均分，新增 Race 后自动重算；玩家改动任一种族滑条后停止自动调整。
- 将“HAR 技术上允许使用”与“该 Race 的原生/扩展异种池”分离；Human 不再显示已归属鼠族、萌螈族等非人类 Race 的异种。
- 非人类 Race 的默认异种只从自身 PawnKind、原生阵营、同源 Xenotype 和 HAR 明确登记的第三方扩展中发现；旧设置中的跨 Race 残留键会被清理。
- 固定领袖不再无条件保留原 Race；存在目标 Race 领袖 PawnKind 时正常替换。
- PawnKind 兼容匹配新增工兵、破墙手与灵能仪式执行者角色约束，避免袭击策略的 post-gear validator 连续失败 70/100 次。
- 在选择需要特定 PawnKind 的袭击策略前检查当前配置 Race 是否具备该角色；不兼容策略会被排除，而不是生成一个人类补位或触发红字。
- HAR 仍为可选软兼容：遵循 HAR 白名单/PawnKind/xenotypeSet 的异种扩展可自动发现，无需为每个扩展模组写硬编码补丁。
- Release 构建成功，0 警告、0 错误；静态测试增加异种所有权、自动均分与特殊袭击角色检查。
## 2026-07-26：修复 Race 比例不生效的阻断缺陷

- 确认设置已正确保存；根因是鼠族被 `useFactionXenotypes` 等值条件全部排除，以及普通队伍预选的人类异种阻止萌螈族候选。
- 删除错误的 `useFactionXenotypes` 和 `preventIdeo` 硬等值限制，保留商人、战斗角色、领袖和年龄等实际角色约束。
- 新增 PawnGroup 线程本地上下文，区分普通队伍软异种与任务硬强制异种。
- 将生成流程改为 Race、PawnKind、Xenotype 原子选择，修复鼠族/萌螈族 100% 时静默回退人类的问题。
- 设置 schema 升级为 3，每个 Race 拥有独立异种池；旧阵营异种设置只迁移到 Human 池。
- 自动从 PawnKind、原生阵营和 HAR 限制发现鼠族、萌螈族等 Race 的默认异种。
- Race 扫描改为从普通 PawnKind 反查，排除 CreepJoiner、尸体、异常尸体、变异体、Boss 和伪 Race。
- 阵营列表新增图标、隐藏/不可用显示开关；活动配置阵营绿色并置顶。
- 新增按阵营汇总的生成请求、成功替换和回退原因统计。
- 更新中英文文本、方案、总文档、修改注意事项、静态测试与第二阶段测试矩阵。
## 2026-07-26：第二阶段全阵营通用化与兼容改造

- 玩家名称改为“阵营种族多样性（Faction Race Diversity）”，核心定位改为面向全部已加载阵营的功能框架；两个旧阵营保留为附带示例。
- 保留旧 PackageID、目录、程序集、设置入口和两个 FactionDef defName，兼容已有设置与存档。
- 设置升级为按阵营独立保存种族和异种权重；旧全局异种权重只迁移到两个示例阵营。
- 新安装时两个示例阵营继续使用第一阶段默认异种预设；外部阵营默认遵循原始规则。
- 新增全 FactionDef、人形 Race 与 PawnKindDef 注册表以及阵营搜索/选择设置界面。
- 新增带所有权检查的 `FactionDef.xenotypeSet` 应用与恢复机制。
- 新增基于现有兼容 PawnKindDef 的 HAR/人形种族选择，不克隆或修改共享 Def。
- 引入 Harmony，最小补丁外层人物生成和异种候选入口，并声明在 HAR 之后运行。
- 与阵营文化多样性划分 Race/Xenotype 与 Ideo 所有权，不修改其文化字段和补丁入口。
- About 增加 Harmony 必需依赖和 HAR 可选加载顺序；中英文设置文本全面更新为 FRD 品牌。
- Release 构建成功，0 警告、0 错误；第二阶段静态测试与游戏内测试文档已更新。
## 2026-07-26：阶段 1 审核修正、百科与复验

- 明确两个阵营在世界生成页默认各 1 个且允许删至 0。
- 为文明/粗野阵营补齐独立领袖称谓；粗野阵营显式使用原版粗野城镇图标。
- 精简 About 玩家文案，不显示开发阶段或内部依赖说明。
- 刷新 `Wiki/` 并为两条 FactionDef 补齐人工“游戏解释”列。
- 将 csproj 的 RimWorld 路径改为相对路径，关闭 Release PDB 输出。
- 新增 `测试/运行静态测试.py`，覆盖 XML、PackageID、阵营继承、翻译、源码门控、构建产物和 Wiki。
- Release 重建成功：0 警告、0 错误；静态测试全部通过；清理 `obj/` 与 PDB。
## 2026-07-26：创建第一阶段实现

- 创建固定目录 `yyyyy_MixedPeoplesFactions`，PackageID 为 `yyyyy.mixedpeoplesfactions`，作者为 `yyyyy`。
- 添加 `MPF_MixedCivil` 与 `MPF_MixedRough` 两个自有 FactionDef。
- 添加 Biotech 条件启用的异种权重设置、启动重建、保存重建和 FactionDef 描述缓存刷新。
- 默认比例按当前存在 XenotypeDef 动态计算：无 Starjack 时 Baseliner 85，有 Starjack 时 Baseliner 82.5。
- AllDefs 遍历统一排除 Baseliner，避免设置列表或概率构建重复。
- `ExposeData` 只负责序列化；默认键初始化放在 Def 加载完成后的启动逻辑。
- 全零权重自动回写 Baseliner 100、其余 0，使 UI 与运行时一致。
- 添加 English 与 ChineseSimplified 翻译。
- 添加开发者模式 DebugAction，用于输出两阵营当前异种概率和执行设置检查。
- 明确不实现 HAR/异星种族克隆，不引用 Harmony 或 Biotech DLL。
- 添加 csproj、模组文档三件套和阶段1详细测试文档。




